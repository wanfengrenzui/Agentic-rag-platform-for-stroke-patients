# V2 康复报告功能使用指南

## 功能概述

该项目现已支持生成专业的多模态康复运动数据分析报告。用户可以通过前端上传骨骼点、IMU 和 EMG 数据，系统会自动：

1. 提取多模态特征
2. 生成专业医学报告
3. 绑定数据证据与文献证据
4. 支持 Markdown 和 JSON 导出

## 快速开始

### 1. 启动后端服务器

```powershell
cd agentic-rag-coding
py -m pip install -r requirements.txt
py -m src.main rebuild  # 可选：重建 FAISS 索引
py -m uvicorn src.api:app --host 127.0.0.1 --port 8000
```

### 2. 启动前端开发服务器

```powershell
cd frontend
npm.cmd install  # 首次需要
npm.cmd run dev -- --host 127.0.0.1
```

### 3. 访问应用

打开浏览器访问：`http://127.0.0.1:5173/v2`

## 前端交互流程

### Tab 1: 数据上传与分析

1. **拖入数据文件**：支持 `.skeleton`、`.csv`（IMU/EMG）、`.xlsx` 等格式
2. **查看数据文件列表**：显示已上传的文件及其大小
3. **点击"生成分析报告"**：触发后端报告生成流程
4. **监控生成过程**：显示实时的 4 步生成过程

### Tab 2: 生成专业报告

1. **点击"生成报告"按钮**
2. **系统自动执行**：
   - 特征提取（骨骼、IMU、EMG）
   - 创建患者案例
   - 生成 10 个标准报告章节
   - 绑定证据
3. **查看报告**：
   - 实时显示完整的 Markdown 报告
   - 显示置信度、质量评分、证据数量等指标
   - 列出所有专业分析结论
4. **下载报告**：点击"下载"按钮保存 Markdown 文件

## 后端 API 端点

### 文件上传

```
POST /api/v2/uploads
```

上传多个运动数据文件。支持格式：`.skeleton`、`.csv`、`.xlsx`、`.json`、`.mat`

### 生成报告

```
POST /api/v2/report/analyze
```

基于已上传的文件生成康复分析报告。返回：
- `report_id`: 报告唯一标识
- `markdown_content`: Markdown 格式的完整报告
- `json_content`: JSON 格式的结构化数据
- `metadata`: 报告元数据（置信度、质量评分等）

### 获取已生成的报告

```
GET /api/v2/report/{report_id}
```

检索之前生成的报告。

```
GET /api/v2/reports
```

列出所有已生成的报告。

## 报告结构

生成的报告包含 10 个标准章节：

1. **基本信息** - 患者/对象的年龄、性别、诊断、病程等
2. **免责声明** - 医学合规措辞
3. **数据质量说明** - 数据模态的完整性和质量评估
4. **步态时空参数分析** - 时间和空间特征分析
5. **骨骼关键点与关节活动度分析** - ROM、对称性等
6. **IMU 传感器信号分析** - 加速度、角速度、不对称指数
7. **肌电图(EMG)信号分析** - 肌肉激活、协同激活、疲劳
8. **证据卡片** - 数据证据和文献证据的详细展示
9. **专业分析与结论** - 经过验证的结论与证据绑定
10. **初步康复意义、风险与局限、后续建议**

## 特征提取指标

### 骨骼特征
- 步频、步数、步行持续时间
- 左右步态对称性
- 关节活动度 (ROM)
- 躯干晃动指标

### IMU 特征
- 加速度幅值（平均值和峰值）
- 角速度峰值
- 步态事件检测
- 左右不对称指数
- 信号质量评分

### EMG 特征
- RMS（均方根）
- MAV（平均绝对值）
- IEMG（积分肌电）
- 峰值激活
- 协同激活指标
- 疲劳指标

## 关键技术特性

### 医学合规性
✅ 所有结论都使用审慎措辞（"数据提示"、"可能存在"等）
✅ 不出现诊断性结论
✅ 包含风险和局限说明
✅ 强制性免责声明

### 证据绑定
✅ 每条专业结论都绑定相应的数据证据
✅ 支持文献证据链接（V1 RAG 集成）
✅ 完整的追溯性

### 导出格式
✅ Markdown：易于分享、阅读和修改
✅ JSON：便于系统集成和进一步处理

## 文件位置

- **后端代码**：`src/multimodal/`
  - `data_models.py` - 数据模型
  - `feature_extractor.py` - 特征提取
  - `report_generator.py` - 报告生成
  - `report_exporter.py` - 格式转换

- **前端代码**：`frontend/src/`
  - `pages/V2MultimodalReport.tsx` - V2 主页面
  - `components/ReportViewer.tsx` - 报告查看器组件
  - `styles.css` - 样式表

- **API 端点**：`src/api.py`
  - `/api/v2/uploads` - 文件上传
  - `/api/v2/report/analyze` - 报告生成
  - `/api/v2/report/{report_id}` - 报告获取
  - `/api/v2/reports` - 报告列表

- **示例数据**：`sample_data/`
  - `skeleton_sample.csv` - 骨骼数据示例
  - `imu_sample.csv` - IMU 数据示例
  - `emg_sample.csv` - EMG 数据示例

- **生成的报告**：`output/`
  - `*.md` - Markdown 格式报告
  - `*.json` - JSON 格式数据

## 下一步改进方向

1. **文献证据集成** - 自动调用 V1 RAG 检索相关论文
2. **高级图表** - 可视化时间序列、对比分析
3. **PDF 导出** - 生成可打印的 PDF 报告
4. **多患者数据库** - 支持患者案例的长期追踪
5. **自定义模板** - 允许医院或诊所定制报告格式
6. **深度学习模型** - 集成更准确的特征提取算法

## 故障排除

### 报告生成失败

1. 检查是否上传了有效的数据文件
2. 确保 pandas 已安装：`pip install pandas`
3. 查看后端日志确定具体错误

### 前端无法连接后端

1. 确保后端服务器运行在 `127.0.0.1:8000`
2. 检查浏览器开发者工具中的网络错误
3. 验证 CORS 配置正确

### 样式不显示

1. 运行 `npm.cmd run build` 重新构建前端
2. 清除浏览器缓存
3. 检查 `frontend/dist/` 是否存在

## 贡献和反馈

欢迎提交 Issues 和 Pull Requests 来改进这个项目！
